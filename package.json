{
  "name": "worklink-smart-marketplace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all -p client:dev server:dev",
    "client:dev": "npm --workspace client run dev",
    "server:dev": "npm --workspace server run dev",
    "lint": "npm --workspaces run lint"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "@google/genai": "^1.40.0",
    "@google/generative-ai": "^0.24.1",
    "nodemailer": "^7.0.11",
    "react-hot-toast": "^2.6.0",
    "stripe": "^20.0.0",
    "vite-plugin-node-polyfills": "^0.25.0"
  }
}
